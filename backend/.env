# backend/.env
ENVIRONMENT=development
LOG_LEVEL=INFO
ENABLE_GPU=false

# Model Configuration  
BASE_MODEL_NAME=gpt2
MODEL_CACHE_DIR=./data/models
FINGERPRINT_DIR=./data/fingerprints

# Security (use default for testing)
FINGERPRINT_ENCRYPTION_KEY=test-key-for-codespaces-development-only

# Hugging Face (optional for now)
HF_TOKEN=

# API
API_HOST=0.0.0.0
API_PORT=8000