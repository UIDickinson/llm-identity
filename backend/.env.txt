ENVIRONMENT=development
LOG_LEVEL=INFO
ENABLE_GPU=false

# Model Configuration
BASE_MODEL_NAME=gpt2
MODEL_CACHE_DIR=./data/models
FINGERPRINT_DIR=./data/fingerprints

# Security
FINGERPRINT_ENCRYPTION_KEY=generate-your-key

# API
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Optional
HF_TOKEN=your-api-key

# Audit Configuration
DEFAULT_AUDIT_SAMPLE_SIZE=10
QUICK_AUDIT_SAMPLE_SIZE=5
DEEP_AUDIT_SAMPLE_SIZE=20
FINGERPRINT_MATCH_THRESHOLD=0.85
FUZZY_MATCH_ENABLED=true

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Performance
MAX_CONCURRENT_AUDITS=1
MODEL_CACHE_SIZE_GB=5
ENABLE_MODEL_QUANTIZATION=true